# Copy this file to .env.local and fill in real values to enable OpenAI-compatible LLM
# Leave these empty to use the built-in Mock LLM provider (default, zero-config demo).

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
